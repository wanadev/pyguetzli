language: python

python:
    - "2.7"
    - "3.4"
    - "3.5"
    - "3.6"
    - "nightly"

dist: trusty
sudo: required

addons:
  apt:
    packages:
    - build-essential

install:
    - pip install .[PIL,dev]

script:
    - flake8 pyguetzli test
    - pytest -v

